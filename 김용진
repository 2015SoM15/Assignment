#include<stdio.h>
#include<string.h>

int main(){
	char sen[100];
	//scanf("%s",sen);
	gets(sen);
	int i;
	//char New = sen[i]; // 왜 얘를 생략해야 프로그램이 잘돌아가는건지 모르겠다. 생략안하면 i가 정의되지 않았다고 뜬다
	for (i=0; sen[i]!=NULL; i++){
		if(sen[i]!=' '){
			printf("%c",sen[i]);
		}
		else
			printf("\n");
	}

	return 0;
}

